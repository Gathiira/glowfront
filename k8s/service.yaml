apiVersion: v1
kind: Service
metadata:
  name: glowfront
  namespace: glowfront
  labels:
    app: glowfront
spec:
  type: NodePort
  selector:
    app: glowfront
  ports:
    - port: 31000
      targetPort: 3000
      nodePort: 31000
      name: http
