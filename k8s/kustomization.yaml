apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: manshade

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

images:
  - name: manshade
    newName: manshade
    newTag: latest
